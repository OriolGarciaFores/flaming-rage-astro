---
import { getCollection } from "astro:content";
import BaseWeb from "../layouts/BaseWeb.astro";
import CardPost from "../components/CardPost.astro";
//import { posts } from "../lib/posts.mjs";

/*let postList;

if (posts.length > 6) {
    postList = posts.slice(posts.length - 6, posts.length);
} else {
    postList = posts;
}*/

const allPosts = await getCollection("blog");
let postList;

if (allPosts.length > 6) {
    postList = allPosts.slice(allPosts.length - 6, allPosts.length);
} else {
    postList = allPosts;
}
---

<BaseWeb title="Flaming Rage">
    <section class="my-12">
        <div class="container max-w-screen-xl">
            <div class="items-center grid gap-24 grid-cols-1 lg:grid-cols-2">
                <p class="lg:text-8xl xl:text-left text-6xl text-center text-flaming">
                    FLAMING RAGE
                </p>
                <img
                    alt="imagen-home"
                    class="hidden lg:block image-home"
                    src="/avatar-alts.svg"
                    decoding="async"
                />
            </div>
        </div>
    </section>
    <section class="lg:my-64">
        <header class="container max-w-screen-xl flex justify-center"><h2 class="text-6xl mb-16">Random topics</h2></header>
        
        <div class="container max-w-screen-xl md:flex md:flex-row-reverse md:gap-16">
            <h2 class="text-6xl mb-16 self-center md:basis-1/2 text-right">TECNOLOGÍAS</h2>
            <div class="relative m-auto max-w-4xl md:basis-1/2">
                <img alt="Colorful Image" 
                height="500" 
                src="/home-tecno.webp" 
                width="500" 
                class="mx-auto w-full bg-gray-500 rounded-lg shadow-lg" 
                decoding="async" 
                loading="lazy">
            </div>
        </div>
    </section>
    <section class="lg:my-64">
        <div class="container max-w-screen-xl md:flex md:flex-row md:gap-16">
            <h2 class="text-6xl mb-16 self-center md:basis-1/2">GUIAS</h2>
            <div class="relative m-auto max-w-4xl md:basis-1/2">
                <img alt="Colorful Image" 
                height="500" 
                src="/home-guia.webp" 
                width="500" 
                class="mx-auto w-full bg-gray-500 rounded-lg shadow-lg" 
                decoding="async" 
                loading="lazy">
            </div>
        </div>
    </section>
    <section class="lg:my-64">
        <div class="container max-w-screen-xl md:flex md:flex-row-reverse md:gap-16">
            <h2 class="text-6xl mb-16 self-center md:basis-1/2 text-right">PROYECTOS</h2>
            <div class="relative m-auto max-w-4xl md:basis-1/2">
                <img alt="Colorful Image" 
                height="500" 
                src="/home-proyectos.webp" 
                width="500" 
                class="mx-auto w-full bg-gray-500 rounded-lg shadow-lg" 
                decoding="async" 
                loading="lazy">
            </div>
        </div>
    </section>
</BaseWeb>

<style>
    .image-home {
        max-width: 530px;
        max-height: 560px;
    }
</style>
